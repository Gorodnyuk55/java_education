package homework.beginner.loops;
   /* Write a program that revers digits in the number 668892645*/
public class task22 {
    public static void main(String[] args) {
        System.out.println(new StringBuilder(668892645+"").reverse());




        /* int number = 668892645;
        System.out.println("number - " + number);

        int i = number;
        String strChar;
        String strNumber = Integer.toString(i % 10); // это нужно для заполнения strNumber для цикла
        i = i / 10;   // заполняем первый символ вне цикла

        while (i != 0) {
            int count = i % 10;
            i = i / 10;
            strChar = Integer.toString(count);
            strNumber += strChar;
        }

        int reversNumber = Integer.parseInt(strNumber);

        System.out.println("revers number - " + reversNumber);
*/
    }
}
